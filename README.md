# CaddyDocker

```bash


# 假设你的 Docker Hub 用户名是 yourusername
docker build -t llinkedlist/caddy-replace:latest .

# 如果想添加其他标签(比如版本号)
docker build -t llinkedlist/caddy-replace:latest -t llinkedlist/caddy-replace:v1.0 .
```
